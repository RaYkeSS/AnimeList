export default function Auth() {
  const styles = {
    divWrapper: "flex gap-5",
  };
  return (
    <div className={styles.divWrapper}>
      <div>Sign in</div>
      <div>Sign up</div>
    </div>
  );
}
